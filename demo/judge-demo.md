# 判定シート — デモアプリ（セミナー申込フォーム）director/medium

- 対象: docs/lt/demo/app.js（spec.md だけを渡してAIが実装）
- run: e80ac92ff11e / 観測10件 → 質問10件
- 生成日: 2026-08-24

> **AI推測を --intent の既定値として埋めてあります**（decisions/0003 の設計どおり）。
> 同意するならそのまま実行、違うなら文言を書き換え、現状が正しいなら confirmed に変えてください。

---

## 1. [score 4] critical / omission

**観測**: カード課金は完了しているが、その後の処理（申込登録・メール送信）が完了しない可能性がある。返金処理は呼び出されない。

**質問**: コードは決済後の後続処理失敗を検知した際に、利用者へのエラー通知の手段が定義されていない（メール送信自体が失敗している）。利用者は申込が成功したのか失敗したのかを知る方法がない状態になりうるが、この「ユーザーへの結果通知がゼロになりうる」状態は意図通りか？

**AI推測**: 意図していない。利用者に何らかの形で処理結果（成功・失敗）を伝える手段を確保すべき。

```bash
npx tsx src/cli/oracle.ts decide 408b7cc99ec9f96d corrected --intent "意図していない。利用者に何らかの形で処理結果（成功・失敗）を伝える手段を確保すべき。"
```

現状が正しいなら → `decide 408b7cc99ec9f96d confirmed`　／　分からないなら → `decide 408b7cc99ec9f96d unknown`

---

## 2. [score 4] money / omission

**観測**: 定員・締切の条件を満たす限り、同一人物からの重複申込が何件でも受け付けられ、その都度課金・登録・メール送信が行われる。

**質問**: コードは同一人物が同じセミナーに何度でも申し込めるようになっており、その都度課金・登録・メール送信が行われる。これは「1人1申込」を業務ルールとする場合に重大なリスクになるが、重複申込を許容しないルールは存在しないのか？

**AI推測**: 意図していない。同一メールアドレス×同一セミナーIDの重複申込は拒否すべき。

```bash
npx tsx src/cli/oracle.ts decide ff48d299d95cd04a corrected --intent "意図していない。同一メールアドレス×同一セミナーIDの重複申込は拒否すべき。"
```

現状が正しいなら → `decide ff48d299d95cd04a confirmed`　／　分からないなら → `decide ff48d299d95cd04a unknown`

---

## 3. [score 4] auth / precondition

**観測**: 該当申込に紐づくカード決済が返金処理され、申込レコードがメモリ上から削除され、申込者のメールアドレス宛にキャンセル完了メールが送信される。レスポンスとして成功を示す値が返される。残席数は1増える。

**質問**: コードはキャンセルリクエストに申込IDを指定するだけで返金・削除が実行される。申込者本人かどうかの確認が存在しないように見えるが、第三者が他人の申込IDを知っていた場合にキャンセル・返金ができてしまう状態は意図通りか？

**AI推測**: 意図していない。キャンセル操作は申込者本人のみが実行できるよう認証・認可を設けるべき。

```bash
npx tsx src/cli/oracle.ts decide 685ed4bd6b9fb3a8 corrected --intent "意図していない。キャンセル操作は申込者本人のみが実行できるよう認証・認可を設けるべき。"
```

現状が正しいなら → `decide 685ed4bd6b9fb3a8 confirmed`　／　分からないなら → `decide 685ed4bd6b9fb3a8 unknown`

---

## 4. [score 4] auth / precondition

**観測**: カード決済が実行され、申込レコードがメモリ上に追加され、申込者のメールアドレス宛に申込完了メールが送信される。レスポンスとして申込IDが返される。

**質問**: コードは申込リクエストを受け付ける際に、申込者が本人であることの確認（ログイン・認証）が存在しないように見える。任意のメールアドレスを指定して他人名義で申込・課金を行える状態は意図通りか？

**AI推測**: 意図していない。申込操作はログイン済みユーザーに限定し、課金先と申込者を一致させるべき。

```bash
npx tsx src/cli/oracle.ts decide 61c98562c852100a corrected --intent "意図していない。申込操作はログイン済みユーザーに限定し、課金先と申込者を一致させるべき。"
```

現状が正しいなら → `decide 61c98562c852100a confirmed`　／　分からないなら → `decide 61c98562c852100a unknown`

---

## 5. [score 4] data / contradiction

**観測**: コードは定員チェックをメモリ上の申込データをもとに行っているが、サーバー再起動時に申込データが全消失するため、再起動後は実際には課金済みの申込者が存在するにもかかわらず定員ゼロとして扱われる。「課金済み申込者が存在するのに残席が満席として計算されない」という矛盾が生じるが、どちらの挙動が正しいのか？

**質問**: コードは定員チェックをメモリ上の申込データをもとに行っているが、サーバー再起動時に申込データが全消失するため、再起動後は実際には課金済みの申込者が存在するにもかかわらず定員ゼロとして扱われる。「課金済み申込者が存在するのに残席が満席として計算されない」という矛盾が生じるが、どちらの挙動が正しいのか？

**AI推測**: データ永続化なしが不具合。申込データを永続化し、再起動後も正確な残席数を維持すべき。

```bash
npx tsx src/cli/oracle.ts decide ec45caa2fb9f538a corrected --intent "データ永続化なしが不具合。申込データを永続化し、再起動後も正確な残席数を維持すべき。"
```

現状が正しいなら → `decide ec45caa2fb9f538a confirmed`　／　分からないなら → `decide ec45caa2fb9f538a unknown`

---

## 6. [score 4] money / boundary

**観測**: 該当申込に紐づくカード決済が返金処理され、申込レコードがメモリ上から削除され、申込者のメールアドレス宛にキャンセル完了メールが送信される。レスポンスとして成功を示す値が返される。残席数は1増える。

**質問**: コードはキャンセル操作に締切日や申込状態のチェックが存在しないように見える。セミナー開催後やすでに返金済みの申込に対してもキャンセル・返金リクエストが何度でも実行できる状態は意図通りか？

**AI推測**: 意図していない。キャンセル可能期間の制限、および二重返金の防止チェックを設けるべき。

```bash
npx tsx src/cli/oracle.ts decide a6360642f17a49a7 corrected --intent "意図していない。キャンセル可能期間の制限、および二重返金の防止チェックを設けるべき。"
```

現状が正しいなら → `decide a6360642f17a49a7 confirmed`　／　分からないなら → `decide a6360642f17a49a7 unknown`

---

## 7. [score 4] data / contradiction

**観測**: コードは残席数をメモリ上の申込データから計算しているが、重複申込チェックがないため同一人物が複数の申込レコードを持て、残席数がその分減る。一方でセミナー一覧の残席数は「実際に参加できる人数」として利用者に提示される。「同一人物の重複申込が残席を圧迫し、他の利用者が申し込めなくなる」という状態は業務として正しいか？

**質問**: コードは残席数をメモリ上の申込データから計算しているが、重複申込チェックがないため同一人物が複数の申込レコードを持て、残席数がその分減る。一方でセミナー一覧の残席数は「実際に参加できる人数」として利用者に提示される。「同一人物の重複申込が残席を圧迫し、他の利用者が申し込めなくなる」という状態は業務として正しいか？

**AI推測**: 意図していない。重複申込を防止することで残席数の正確性を担保すべき。

```bash
npx tsx src/cli/oracle.ts decide 158f65ba91e77cac corrected --intent "意図していない。重複申込を防止することで残席数の正確性を担保すべき。"
```

現状が正しいなら → `decide 158f65ba91e77cac confirmed`　／　分からないなら → `decide 158f65ba91e77cac unknown`

---

## 8. [score 2] money / behavior

**観測**: カード課金は完了しているが、その後の処理（申込登録・メール送信）が完了しない可能性がある。返金処理は呼び出されない。

**質問**: コードは「決済成功後にメール送信等が失敗した場合、返金を行わない」という挙動になっている。しかし利用者はカード引き落としが発生しているにもかかわらず申込が完了しておらず、返金も受けられない状態になりうる。この「課金されたが申込なし・返金なし」という状態は意図通りか？

**AI推測**: 意図していない。決済成功後に後続処理が失敗した場合は自動返金すべき。

```bash
npx tsx src/cli/oracle.ts decide 50ab90e71b947775 corrected --intent "意図していない。決済成功後に後続処理が失敗した場合は自動返金すべき。"
```

現状が正しいなら → `decide 50ab90e71b947775 confirmed`　／　分からないなら → `decide 50ab90e71b947775 unknown`

---

## 9. [score 2] critical / behavior

**観測**: 全申込データが消滅する。再起動後は申込件数がゼロとなり、全セミナーの残席が定員と同数に戻る。

**質問**: コードはすべての申込データをメモリ上にのみ保持しており、サーバー再起動で全消失する。再起動後は「課金済みだが申込記録なし・返金なし」の利用者が発生しうるが、この設計は本番運用を想定したものとして意図通りか？

**AI推測**: 意図していない。申込データはデータベース等の永続ストレージに保存すべき。

```bash
npx tsx src/cli/oracle.ts decide 235c00a2673f31bb corrected --intent "意図していない。申込データはデータベース等の永続ストレージに保存すべき。"
```

現状が正しいなら → `decide 235c00a2673f31bb confirmed`　／　分からないなら → `decide 235c00a2673f31bb unknown`

---

## 10. [score 2] other / boundary

**観測**: 「申込期限を過ぎています」というメッセージとHTTP 400が返される。処理はここで終了する。

**質問**: コードは締切チェックを「サーバー日付が締切日より後か」で判定しているが、締切日当日（同日）の扱いが「申込可能」と「申込不可」のどちらになっているか業務として決定されているか？

**AI推測**: 締切日当日は申込可能として扱うべき（当日23:59まで受付）。

```bash
npx tsx src/cli/oracle.ts decide 2858514dc0b422a4 corrected --intent "締切日当日は申込可能として扱うべき（当日23:59まで受付）。"
```

現状が正しいなら → `decide 2858514dc0b422a4 confirmed`　／　分からないなら → `decide 2858514dc0b422a4 unknown`

---

