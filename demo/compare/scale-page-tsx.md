# 規模を変えた探り — page.tsx

- 対象: `app/project/page.tsx`（732行 / 38201字 / 識別子 283種）
- レンズ: director/medium / model: claude-sonnet-4-6
- 指標: **識別子混入率**（質問文にソース中の識別子が1つでも含まれる割合）。AIには判定させず機械的に計数。

| 条件 | 入力 | 質問数 | 識別子を含む | 混入率 |
|---|---|---|---|---|
| **C** | 観測リストのみ | 17 | 12 | **71%** |
| **B** | コード本体 | 13 | 10 | **77%** |

## 識別子が入っていた質問

- [C（コードなし）] removeMemberAction … コードは「削除ボタンを押すと即座にremoveMemberActionが呼び出される」になっている。しかし、削除は取り消せない業務操作であり
- [C（コードなし）] owner, iCanManage … コードは「iCanManage = true なら誰でも削除できる」になっている。しかし、オーナー（owner権限のメンバー）を削除できるか
- [C（コードなし）] addMemberAction … コードは「メールアドレスを入力して追加ボタンを押すとaddMemberActionが呼び出される」になっている。しかし、すでにプロジェクトの
- [C（コードなし）] memberr, notfound … コードは「?memberr=notfound というクエリパラメータがあるときにエラーメッセージを表示する」になっている。しかしURLクエリ
- [C（コードなし）] saveProjectAction … コードは「保存中…に変わる」になっている。しかし、saveProjectActionが失敗したとき（通信エラーや権限エラー等）にユーザーへ何
- [C（コードなし）] saveProjectLessonsAction … コードは「saveProjectLessonsActionが失敗したときの挙動」が仕様に記載されていない。保存ボタンが「保存中…」のまま固ま
- [C（コードなし）] distillLessonsAction, meta, lessons … コードは「distillLessonsActionが外部処理+DB書込をする」になっている。しかし、抽出に失敗したとき（外部API障害等）に
- [C（コードなし）] iCanEdit, Excel … コードは「Excel出力リンクが常時表示される」になっている。しかし、閲覧のみ権限（iCanEdit = false）のユーザーでもExce
- [C（コードなし）] ledger, close … コードは「正常終了・例外発生のいずれでもledger.close()が実行される」になっている。しかし、close()自体が例外を投げた場合
- [C（コードなし）] meta, correctedTotal, lessons … コードは「correctedTotal > 0 または meta.lessons が設定されているとき学習ナレッジパネルが表示される」になっ
- [C（コードなし）] addMemberAction, memberr, notfound … コードは「addMemberActionが失敗したとき（存在しないメール以外のエラー）にユーザーへ何を表示するか」が仕様に記載されていない（
- [C（コードなし）] ProjectPage, iCanEdit, issue, projectKey … コードは「iCanEdit = true のとき課題を立てるボタンが表示され /issue/new?projectKey=...&p=...
- [B（コードあり）] meta, lessons … コードは「訂正から教訓を抽出」ボタンを押すと確認なしに既存の学習ナレッジを上書きする（meta?.lessonsを置き換える）可能性がある。
- [B（コードあり）] member, owner, iCanManage … コードはiCanManageを持つユーザーが全メンバーのロールを変更できる。しかし自分自身のロールをownerからmemberに降格できてし
- [B（コードあり）] hidden, createdAt … コードはcreatedAtをhiddenフィールドでフォームに埋め込みサーバーアクションに送信している。しかし画面上でこの値を改ざんされた場
- [B（コードあり）] meta, repo, GitHub … コードはmeta?.repoが存在するだけで「連携中」と緑バッジを表示している。しかし実際にGitHub APIへの接続が有効かどうかを検証
- [B（コードあり）] viewer, iCanEdit, iCanManage, Excel … コードはExcel出力リンクをiCanEdit・iCanManageに関係なく全ロール（viewerも含む）に表示している。しかしviewe
- [B（コードあり）] corrected, unknown, preTest … コードは「テスト前に見つけた問題」をcorrected（✗訂正）＋unknown（?不明）の合計として大きく表示している。しかし「不明」はま
- [B（コードあり）] userById … コードは担当者IDがuserById（現在のメンバー一覧）に存在しない場合、IDそのものをname列に表示している。しかし退会・削除済みユー
- [B（コードあり）] project … コードの課題別テーブルのリンク先が /project ページではなく / （ルートページ）になっている。しかし /project ページ内の
- [B（コードあり）] saveProjectAction, viewer, iCanEdit, iCanManage … コードは設定フォーム（saveProjectAction）をiCanEditのときだけ表示しているが、メンバーセクション（<details>
- [B（コードあり）] oracle, track, status, corrected … コードはcorrectedTotal（学習ナレッジの元数）を「track問わずstatus=corrected かつ declaredInt

## C の質問（全件）

1. コードは「アクセス可能なプロジェクトが1件以上あれば先頭プロジェクトへフォールバックする」になっている。しかし、ユーザーが意図的に存在しないプロジェクトキーを指定した場合に404ではなく無言で別プロジェクトへ飛ぶのは、誤操作・なりすましリンクによる混乱を招く恐れがある。これは意図通りか？
2. コードは「権限のないプロジェクトはツリーから除外される」になっている。しかし、URLに直接プロジェクトIDを入力してアクセスしようとした場合に、ツリー除外だけで本当にアクセスがブロックされるのか、それとも別途サーバー側でのアクセス拒否処理があるのかが不明。UIからの動線以外でデータが見えてしまう可能性はないか？
3. コードは「削除ボタンを押すと即座にremoveMemberActionが呼び出される」になっている。しかし、削除は取り消せない業務操作であり、確認ダイアログなしで実行されるのは誤操作リスクが高い。削除前に確認ステップは存在するか？
4. コードは「iCanManage = true なら誰でも削除できる」になっている。しかし、オーナー（owner権限のメンバー）を削除できるか否かが仕様に記載されていない。自分自身を削除できる場合、管理者が誰もいなくなるプロジェクトが生まれる可能性はないか？
5. コードは「メールアドレスを入力して追加ボタンを押すとaddMemberActionが呼び出される」になっている。しかし、すでにプロジェクトのメンバーであるユーザーのメールを入力した場合の挙動が仕様に記載されていない。重複追加された場合、どうなるか？
6. コードは「?memberr=notfound というクエリパラメータがあるときにエラーメッセージを表示する」になっている。しかしURLクエリでエラー状態を管理する設計では、ユーザーがそのURLをブックマークしたり共有したりした場合に、エラーが意図せず再表示され続ける。これは意図通りか？
7. コードは「保存中…に変わる」になっている。しかし、saveProjectActionが失敗したとき（通信エラーや権限エラー等）にユーザーへ何を表示するかが仕様に記載されていない。失敗しても保存できたと誤認する可能性はないか？
8. コードは「saveProjectLessonsActionが失敗したときの挙動」が仕様に記載されていない。保存ボタンが「保存中…」のまま固まる可能性はないか？
9. コードは「distillLessonsActionが外部処理+DB書込をする」になっている。しかし、抽出に失敗したとき（外部API障害等）にユーザーへ何を表示するかが仕様に記載されていない。また、既存のmeta.lessonsが上書きされる場合、元の教訓テキストは失われるが、その旨の警告はあるか？
10. コードは「ロール変更セレクトが表示される」になっている。しかし、ロール変更を確定する操作（ボタン押下など）があるのか、それともセレクトの選択と同時に即時反映されるのかが仕様に記載されていない。即時反映の場合、誤操作時に取り消す手段はあるか？
11. コードは「Excel出力リンクが常時表示される」になっている。しかし、閲覧のみ権限（iCanEdit = false）のユーザーでもExcel出力が可能な設計になっている。これは意図通りか？また、機密性の高いプロジェクト情報をExcelとして誰でもダウンロードできることに業務上の問題はないか？
12. コードは「採用済みテスト項目が0件のとき『候補のみ』と表示される」になっている。しかし、担当者別テーブルでは合格数・未実施数が0のとき空白表示になる（0と表示されない）という別の表示ルールがある。ゼロ値の表現方針がテーブルによって異なるが、これは意図的な使い分けか、それとも一方が誤りか？
13. コードは「正常終了・例外発生のいずれでもledger.close()が実行される」になっている。しかし、close()自体が例外を投げた場合の挙動が仕様に記載されていない。close()失敗によって接続がリークし、後続のユーザー操作が全員影響を受ける可能性はないか？
14. コードは「権限のないプロジェクトはツリーから除外」かつ「フォールバック時はアクセス可能なプロジェクトの先頭を表示する」になっている。しかし、権限チェックとフォールバックの処理順序が仕様から不明なため、権限のないプロジェクトが先頭に来た場合に意図せずそのプロジェクトが表示される可能性はないか？これは矛盾していないか？
15. コードは「correctedTotal > 0 または meta.lessons が設定されているとき学習ナレッジパネルが表示される」になっている。しかし、correctedTotalはページロード時点の値であり、別ユーザーが訂正を追加しても即時反映されない可能性がある。ページを再読込しない限りパネルが表示されないケースは業務上問題ないか？
16. コードは「addMemberActionが失敗したとき（存在しないメール以外のエラー）にユーザーへ何を表示するか」が仕様に記載されていない（?memberr=notfound は1種類のエラーしかカバーしていない）。権限エラーやDB障害など他のエラー時にボタンが「追加中…」のまま固まらないか？
17. コードは「iCanEdit = true のとき課題を立てるボタンが表示され /issue/new?projectKey=...&p=... へ遷移する」になっている。しかし、遷移先の /issue/new でも同様のアクセス制御が行われているかが不明。ProjectPage側のボタン非表示だけがガードであれば、URLを直打ちすることで編集権限のないユーザーが課題を作成できる可能性はないか？

## B の質問（全件）

1. コードはURLに ?p= が指定されていない場合、またはIDが見つからない場合にツリーの先頭プロジェクトを自動表示している。しかし意図せず別プロジェクトのデータを閲覧させてしまうリスクがある。IDが不正・未指定のときは先頭プロジェクトにフォールバックするのではなく、エラー表示やリダイレクトを行うのが正しいか？
2. コードはメンバー削除ボタンを押すと確認なしに即時削除が実行される。しかしオーナーが誤操作で自分自身や他のオーナーを削除した場合、プロジェクトの管理者不在になるリスクがある。削除前に確認ダイアログ、もしくは最後のオーナーの削除を防ぐガードが存在しないのは意図通りか？
3. コードは「訂正から教訓を抽出」ボタンを押すと確認なしに既存の学習ナレッジを上書きする（meta?.lessonsを置き換える）可能性がある。しかし人間が手で編集・蓄積したナレッジが意図せず消えることはビジネス上の損失になりうる。既存のナレッジがある場合は上書き前に警告またはマージ確認を行うのが正しいか？
4. コードはすでにメンバーである人のメールアドレスを再度入力してもエラーを出す仕組みが画面上に見当たらない。しかし重複追加によってロールが上書き・二重登録されると権限管理の整合が崩れる。重複追加時の業務上の動作（上書き・拒否・エラー通知）はどれが正しいか？
5. コードはiCanManageを持つユーザーが全メンバーのロールを変更できる。しかし自分自身のロールをownerからmemberに降格できてしまう場合、管理者不在になるリスクがある。自分自身のロール変更、または最後のownerのロール降格を防ぐガードは意図通り存在しないか？
6. コードはcreatedAtをhiddenフィールドでフォームに埋め込みサーバーアクションに送信している。しかし画面上でこの値を改ざんされた場合、作成日時が意図しない値で上書きされるリスクがある。createdAtはサーバー側で保持・検証すべきではなく、クライアントから受け取った値をそのまま信頼するのは意図通りか？
7. コードはmeta?.repoが存在するだけで「連携中」と緑バッジを表示している。しかし実際にGitHub APIへの接続が有効かどうかを検証していない可能性があり、接続が切れていても「連携中」と表示され続けるリスクがある。「連携中」バッジはリポジトリURL保存の有無だけを示すものとして意図されているか、それとも実際の接続状態を示すべきか？
8. コードはExcel出力リンクをiCanEdit・iCanManageに関係なく全ロール（viewerも含む）に表示している。しかしviewerが全テストデータをExcelとして持ち出せることが業務上許容されているかどうか確認が必要。Excel出力は閲覧権限があれば全員に許可する設計で意図通りか？
9. コードは「テスト前に見つけた問題」をcorrected（✗訂正）＋unknown（?不明）の合計として大きく表示している。しかし「不明」はまだ問題と確定していない状態であり、確定済みバグと未確定の疑念を同列に「問題数」として合算することは業務上の誤解（実績の過大評価）を生む可能性がある。unknownをpreTestに含めるのは意図通りか？
10. コードは担当者IDがuserById（現在のメンバー一覧）に存在しない場合、IDそのものをname列に表示している。しかし退会・削除済みユーザーやシステム外IDが担当者として表示されると、業務上誰が担当なのか判別できない。退会済みユーザーの担当テスト項目をどう業務上扱うべきか（表示・再割当催促など）は意図が定まっているか？
11. コードの課題別テーブルのリンク先が /project ページではなく / （ルートページ）になっている。しかし /project ページ内の課題別リンクがルートに飛ぶのはユーザーにとって予期しない画面遷移であり、意図されたものか疑わしい。課題へのリンク先は /project?… ではなく / が正しい遷移先として意図されているか？
12. コードは設定フォーム（saveProjectAction）をiCanEditのときだけ表示しているが、メンバーセクション（<details>）はiCanEdit・iCanManageに関係なく常に表示（summary内でiCanManageを判定）している。しかし権限がないviewerにもメンバー一覧（氏名・メール・最終ログイン時刻）が見えることになる。メンバー情報の閲覧をviewerに許可するのは意図通りか？
13. コードはcorrectedTotal（学習ナレッジの元数）を「track問わずstatus=corrected かつ declaredIntentあり」で数えているが、findingItems（見つかった問題の中身）はtrack=oracleのstatus=corrected|unknownで絞っている。どちらかの定義が業務意図と合っていない可能性がある。「訂正の資産化」の母数はoracleトラックに限定すべきか、全トラックを対象とすべきか、どちらが正しいか？
