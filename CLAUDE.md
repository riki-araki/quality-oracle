# CLAUDE.md

このファイルは Claude Code が毎セッション自動で読む。プロジェクトの前提とガードレールを定義する。

## まず読むもの（順番厳守）
1. `docs/design.md` — 設計の本体。ここが正典。判断に迷ったらここに従う（§5.1 は 0002 で更新済み）。
2. `decisions/` — 主要判断と「捨てた選択肢」。**0001→0005 まで全て**目を通す（前提が更新されている）。
3. `docs/prompts/pass1-observation.md` / `pass2-interrogation.md` / `pass3-testgen.md` — エンジンの雛形（単一の真実源）。
4. `README.md` の「現在地」「使い方」「ダッシュボード」 — 今動くものの全体像。

## このプロジェクトの一行要約
AIが書いたコードに対し、**AIは観測と問いだけをし、人間は正解の承認だけをする**ことで、
「テストの採点基準をAIが作る」循環参照を断ち、品質を担保するツール。核は「オラクルの引き出し」。

## 不変の原則（破らない）
- **スコープ規律が最優先。** 最大のリスクは「作り込みすぎて完成しないこと」（design.md §5.3）。
  機能を増やす前に「核（オラクル引き出し）の価値」を優先する。新領域は**勝手に広げず先に確認**。
- **観測と尋問は必ず分離する**（Pass1 と Pass2 を別API呼び出しに。§6.4）。一発でやらない。
- **鏡化を防ぐ**（「コードを読めば答えられる質問」は不可。§6.3）。
- **✓ ＝「現状の挙動が正しい」**（declaredIntent＝観測→回帰）、**✗＝訂正**（→既知バグ）。AI推測は✗訂正欄の既定値のみ（decisions/0003）。
- **テストはコードでなく「承認済みの意図」から生成する**（§1.3 の循環参照を踏まない）。
- **ローカル優先・データは既定で外に出さない**（§5.1）。ホスト送信は明示オプトイン（0002）。
- **設計・戦略を変える判断をしたら `decisions/` に1ファイル追記**（消さない・ステータス更新）。

## スコープ現況（2026-06-28 時点）
**解禁済み（実装済み or 進行中）:**
- 承認/管理ダッシュボード（Next.js + Tailwind・ローカル）。項目はテーブル＋詳細画面 = decisions/0005, 0006。
- GitHub を**読み取り専用の入力**として使用（PR/ファイル → 観測）= decisions/0004。
- **証跡（画像）をローカル保存**（`data/evidence/`・項目詳細から）= decisions/0006。
- **観点のレンズ**: 主体（director 業務 / engineer 技術）× 強度（loose/medium/strong）= decisions/0007。
  レンズ定義は `docs/prompts/lenses.md`（単一の真実源）。
- **候補→採用のキュレーション**（adopted フラグ）と **UIからの観測起動**（＋項目追加）= decisions/0008。
  最終成果物は「テスト項目」、AIの観測/質問はその生成のための候補。
- **テスト項目ごとの実行結果（手動）を履歴で記録**（pass/fail/blocked・未実施）= decisions/0009。
  ※人手の結果記録であり、テストの「自動実行」ではない（ガードレール非抵触）。
- **課題の一級エンティティ化**（タイトル/概要/AI分析ナレッジ/項目）＋プロジェクトから課題作成 = decisions/0010。
  **AI分析ナレッジ**（概要＋ソースから下書き）を観測/尋問プロンプトの `<前提知識>` に注入する。
- プロジェクト横断ダッシュボード（`/project`・合格/不合格/未実施・主体別）。
- **プロジェクトの一級化**（作成・複数リポ束ね）＋レコードを課題へ明示リンク（issue_id）= decisions/0012。
- **Excel出力**（課題=1シート・証跡画像/送受信埋込）= decisions/0011, 0015。
- **GitHubリポジトリ連携 / private対応（gh認証）/ デグレ観点の補強** = decisions/0013。
- **項目の手動HTTPコンソール**（人が送り人が判定）＋**リクエストのAI下書き** = decisions/0014, 0015。
  検証手段は項目詳細のタブ（リクエスト=API / 画面化=スクショ）。
- **種別(track)で由来を区別**（oracle/normal/manual/imported）= decisions/0016。
  正常系(normal)＝観測から生成する**現状固定の回帰**項目（候補止まり・全部採用ボタン）。手動追加・JSON取込も可。
  §6.3アンチミラーは「**質問**」に効かせ「**項目**」には効かせない（正しさは人の✗で表明）。
- **項目の優先度**（priority 高/中/低）= decisions/0017。risk の category とは別軸の実施順。
  既定は risk から導出（`priorityFromRisk`）、人が項目詳細で変更。サイドバーは Project 単位で開閉。
- **項目の担当者**（assignee・userId）= decisions/0019。実施者(results.created_by)とは別＝割り当て。
  プロジェクトメンバーから選ぶ。一覧に「担当」列＋「自分の担当だけ」フィルタ。
- **マルチユーザー化（認証・ユーザー管理・データ分離）= decisions/0018。§8 を解禁・段階導入**。
  認証＝Auth.js v5 + メール/パスワード（`crypto.scrypt`）。**Next.js 16 はルート保護を `proxy.ts`**（旧 middleware）。
  **Phase1（実装済）**: ログイン/サインアップ・全ルート保護・現在ユーザー表示・結果への記名(created_by)。
  **Phase2（実装済）**: project_members でデータ分離・メンバー管理(招待制)・ロール権限。
  権限＝admin(全)/owner(管理)/member(作業)/viewer(閲覧)。判定は `app/lib/access.ts` に集約。既存は最初のadmin所有にバックフィル。
  **Phase3（未）**: ホスティング＋PostgreSQL 移行（別 decision）。`AUTH_SECRET` は `.env`（ハードコード禁止）。
  ※`auth.config.ts` に `trustHost: true` 設定済み（本番/ホスティングで UntrustedHost を回避）。最終ログイン(last_seen_at)表示あり。
- **一覧の一括操作**（複数選択 → 担当割当 / 合否記録。`bulkAssignAction`/`bulkRecordResultAction`・各レコードの権限を確認）。
- **テスト前検出ダッシュボード（§6.10③ をUI化）**: `/project` で **✗訂正率**・リスク領域別/課題別・**見つかった問題の中身**（観測 vs 人の訂正）。
  集計は `track=oracle` のみ（正常系/手動/取込は人の判断由来でないため除外）。これがツールの価値の直接指標。
- **UIデザインシステム = decisions/0020**（Vercel/Geist・ライト/ダーク テーマトークン・統一ラインアイコン・⌘Kパレット・トースト/スケルトン）。
  **色は hex 直書き禁止**（`globals.css` の `:root`/`.dark` トークン or `var(--…)`。例外＝Excel の ARGB）。
- **AI使用状況の可視化**（`/ai`・管理者のみ）= decisions/0021。LLM呼び出しの回数/トークン/概算コストを種別×モデル×ユーザー×プロジェクトで集計。
  記録は `callModel`（pipeline.ts）一点集約 → `ledger.ai_usage`。読み取り専用の可視化でガードレール非抵触。単価は概算（実請求はコンソール）。
- **意図ドリフト検出**（§6.2⑥のループを閉じる）= decisions/0022。承認済みの意図（adopted＋declaredIntent）と現在コードの再観測を
  突き合わせ、矛盾（ドリフト）だけを検出（`checkDrift`・プロンプト `drift-check.md`・`ledger.drift_findings`）。判定は人（「新挙動を正とする→意図更新+テスト再生成」/「無視」）。AIは矛盾提示まで＝観測と承認の分離を維持。UIは課題画面の「意図ドリフト」パネル。
- **訂正の資産化**（学習ナレッジ）= decisions/0023。プロジェクト配下の ✗訂正（observation→declaredIntent）から再利用可能な教訓を抽出
  （`distillLessons`・プロンプト `lessons-distill.md`・`project_meta.lessons`）。`loadKnowledge` がプロジェクト学習ナレッジ＋課題ナレッジを結合し、観測/尋問/正常系/ドリフトの `<前提知識>` に自動注入（使うほど質問が賢くなる）。人が編集して育てる。UIは `/project` の「学習ナレッジ（訂正から）」パネル。
- **質問の情報量で並べる**（✓スルー抑止）= decisions/0024。候補レビュー（/add）でオラクル質問を当たりそうな順（`infoScore`＝低確信/矛盾・抜け/高リスク）に並べ、鏡に近い低情報（`isLowInfo`＝高確信・挙動・低リスク）は畳む。`/project` に「レンズ別の当たり率」（主体×強度が実際に ✗/? を生んだ割合）。LLM追加呼び出しなし（既存記録から計算）。
- 将来のホスト型SaaSを視野に入れたスタック（Next.js）= decisions/0002。

**まだ作らない（明示的に指示・decisions 化されるまで）:**
- **テストの「自動実行」機能**（自動でテストを走らせ機械が合否判定するランナー）。
  ※項目の**手動HTTPコンソール**（人が送り人が判定）は許可済み = decisions/0014。
- 単体・結合テスト向け生成（E2E/受け入れ優先。単体・結合は北極星）。
- GitHub への**書き戻し**（PRコメント/チェック）、Backlog 連携、その他の外部連携。
- **ホスティング基盤・PostgreSQL 移行・証跡の S3 等クラウド保存**（まずローカル＝0006）。
  → マルチユーザーの認証/分離は decisions/0018 で解禁したが、**ホスト化と Postgres は Phase3（別 decision）**。
  → design.md §8 の「後回し構想」に捕捉済み。核の検証後・ホスト化と同時に順序立てる。

## 技術メモ
- **スタック**: Node 20+ / TypeScript / Next.js(App Router) + Tailwind / ローカル台帳は `node:sqlite`
  （リポジトリ抽象の背後。将来 PostgreSQL へ差替可）/ データ契約は Zod（`src/engine/schema.ts`）。
  engine は UI/CLI 非依存のモジュール（拡張の継ぎ目）。重い依存は避け、各スライスで必要な分だけ足す。
- LLM 呼び出しは Anthropic API。**モデル名・SDK・API仕様は記憶に頼らず必ず最新を確認**:
  - API: https://docs.claude.com/en/api/overview ／ Docs Map: https://docs.claude.com/en/docs_site_map.md
  - 既定モデル `claude-sonnet-4-6`（env `ORACLE_MODEL` で上書き）。
- API キー・`GITHUB_TOKEN` は環境変数（`.env`）で受け取り、**絶対ハードコードしない**（`.env` は `.gitignore` 済み）。

## コーディング方針
- 「動く最小」を優先。抽象化・汎用化は需要が見えてから。
- プロンプトは `docs/prompts/` の雛形を単一の真実源とし、コードからはそれを読み込む（二重管理しない）。
- ドキュメントは Markdown。差分が読みやすい形で書く。
- **色は必ずテーマトークンを使う（hex 直書き禁止）**。ライト/ダーク切替対応のため `app/globals.css` の
  `:root`/`.dark` で定義した CSS 変数 ＋ Tailwind ユーティリティ（`bg-panel`/`text-ink`/`text-ok`/`bg-input`/
  `text-on-accent` 等）を使う。インライン `style` の色は `var(--ok-solid)` のように変数参照にする
  （`app/lib/ui.ts` の色マップも同様）。**例外は Excel 出力（`export.ts`）の ARGB**（スプレッドシートにテーマ無し）。
  切替は `app/lib/theme-toggle.tsx`（html の `.dark` 付け外し＋localStorage）、FOUC 防止は `layout.tsx` の inline script。

## 現在地（完了済みマイルストーン）
核（オラクル引き出し）を中心に、**手動テストのワークフロー一式**が動く（ローカルWebアプリ＋CLI）：
**プロジェクト（リポジトリ連携）→ 課題（PR/範囲・概要・AI分析ナレッジ）→ 観測/尋問（主体×強度×デグレ）→
候補の採用 → テスト項目（生きた仕様＋回帰/既知バグ）→ 担当割当（一括可）→ 結果記録（リクエスト/画面化タブ・証跡）→
ダッシュボード可視化 → テスト前検出(✗訂正率)で価値の可視化 → Excel出力**。
マルチユーザー（認証/権限/分離・Phase1-2）と、UIデザインシステム（Vercel/Geist・ライト/ダーク・⌘K）も実装済み。
核は実コードで検証済み（鏡化ほぼゼロ・観点網羅・矛盾検出・実バグ検出、§6.10①②）。✗訂正率(③)はUIで可視化済み。
残る本丸は **§6.10③（✗訂正率）の実PR運用**と、**✓スルー（§6.7）を抑止するUX**の磨き込み、**Phase3（ホスティング＋Postgres）**。詳細は README / decisions/。
